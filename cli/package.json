{
  "name": "maildte-pull",
  "version": "0.1.0",
  "description": "Agente cliente de exportación incremental para MailDTE Collector (RF-07)",
  "private": true,
  "license": "UNLICENSED",
  "bin": {
    "maildte-pull": "dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "@types/unzipper": "^0.10.10",
    "typescript": "^5.7.2"
  }
}
